package net.onelitefeather.vulpes.backend.domain.sound;

/**
 * The relations of a {@link net.onelitefeather.vulpes.api.model.sound.SoundEventEntity} that can
 * be selectively copied alongside its own fields.
 */
public enum SoundRelation {
    SOURCES
}
